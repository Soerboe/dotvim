README (dotvim.git)

Installation:
j    
    gitt clone http://github.com/soerboe/dotvim.git ~/.vim

Config:

    rm ~/.vimrc 
    ln -s ~/.vim/vimrc ~/.vimrc 

Updates:

    cd ~/.vim 
    git pull

